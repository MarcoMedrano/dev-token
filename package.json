{
  "scripts": {
    "compile": "truffle compile",
    "deploy": "truffle migrate",
    "migrate-testnet": "set NODE_OPTIONS='--openssl-legacy-provider' & truffle migrate --network testnet",
    "cp-contract": "cp build/contracts/DevToken.json ./dapp/public"
  },
  "dependencies": {
    "@truffle/hdwallet-provider": "^2.0.8",
    "chai": "^4.3.6",
    "truffle-assertions": "^0.9.2"
  }
}
